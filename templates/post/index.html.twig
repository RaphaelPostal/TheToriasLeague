
{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <p>{{ post.content }}</p>
    <p>Date de création : {{ post.dateCreated|date('d/m/Y') }}</p>
{% endblock %}